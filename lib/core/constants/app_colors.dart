import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 🎨 ألوان هادية ومناسبة للتطبيقات
  static const Color background = Color(0xFFF8F9FC);      // خلفية رمادية فاتحة جداً
  static const Color surface = Color(0xFFFFFFFF);          // سطح أبيض نقي
  static const Color cardBg = Color(0xFFFFFFFF);           // خلفية البطاقات
  
  // الأزرق الرئيسي - هادئ ومحترف
  static const Color primary = Color(0xFF4A90D9);          // أزرق متوسط
  static const Color primaryLight = Color(0xFFEBF2FA);     // أزرق فاتح جداً
  static const Color primaryDark = Color(0xFF357ABD);      // أزرق داكن
  
  // ألوان ثانوية
  static const Color accent = Color(0xFF5B8DEF);           // أزرق مائل للبنفسجي
  static const Color success = Color(0xFF4CAF50);          // أخضر نجاح
  static const Color warning = Color(0xFFFFA726);          // برتقالي تحذير
  
  // النصوص
  static const Color textPrimary = Color(0xFF2D3142);      // نص رئيسي داكن
  static const Color textSecondary = Color(0xFF9CA3AF);   // نص ثانوي رمادي
  static const Color textMuted = Color(0xFFB0B7C3);        // نص خافت
  
  // الحدود والفواصل
  static const Color border = Color(0xFFE5E7EB);          // حدود خفيفة
  static const Color divider = Color(0xFFF3F4F6);         // فاصل
  
  // ألوان الحالة
  static const Color error = Color(0xFFEF5350);
}