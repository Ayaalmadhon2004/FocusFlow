name: focus_app
description: "A new Flutter project for focus timer."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # هذه المكتبة الأساسية التي نستخدمها لإدارة الحالة (State Management)
  flutter_riverpod: ^2.4.9 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # هنا سنضيف الصور لاحقاً بهذا الشكل:
  # assets:
  #   - assets/images/